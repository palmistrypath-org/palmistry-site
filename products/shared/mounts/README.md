# Mounts lesson diagrams

These `.svg` files are **generated**. Do not edit them by hand.

Source: `scripts/generate-product-diagrams.mjs` (run `node scripts/generate-product-diagrams.mjs`; it also writes `../diagrams/`). One shared engraved hand (`products/shared/palm.mjs`) in the black/gold/purple language; the highlighted mount gets topographic gold rings and a violet bloom, the other regions stay as dotted outlines, and labels use leader lines. Each file is referenced by the product sources under `products/`.

See `docs/visual-system.md` for the visual language and `docs/mount-diagram-creative-briefs.md` for the original per-diagram content requirements.
